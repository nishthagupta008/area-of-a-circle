#1. AREA OF CIRCLE
from math import pi
radius= float(input(" Input the radius of the circle"))
area= pi*radius**2
print("the area of radius of the circle with given value is:" +str(area))







#2. FILENAME EXTENTION
f = input("Input the Filename: ")
filex = filename.split(".")
print ("Extension of the file is " + repr(filex[-1]))
